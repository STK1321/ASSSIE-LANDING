---
const { class: className = "" } = Astro.props;
---
<span class={`inline-flex items-center rounded-full border border-slate/20 bg-white/70 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-slate ${className}`}>
  <slot />
</span>
