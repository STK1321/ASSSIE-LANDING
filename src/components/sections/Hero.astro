---
import Button from "@/components/atoms/Button.astro";
import Icon from "@/components/atoms/Icon.astro";
import { site } from "@/content/site";

const badges = [
  { label: "SEO + Performance", href: "#seo" },
  { label: "Automatización + IA", href: "#servicios" },
  { label: "Desarrollo a medida", href: "#servicios" }
];
const whatsappLink = `https://wa.me/${site.whatsapp.replace(/\D/g, "")}`;
---
<section id="top" class="relative overflow-hidden bg-hero">
  <div class="pointer-events-none absolute inset-0 bg-grid opacity-30" aria-hidden="true"></div>
  <div class="mx-auto flex max-w-6xl flex-col gap-12 px-6 pb-20 pt-32 lg:flex-row lg:items-center">
    <div class="flex-1 space-y-6 reveal">
      <div class="flex flex-wrap gap-2">
        {badges.map((badge) => (
          <a
            href={badge.href}
            class="inline-flex items-center rounded-full border border-slate/20 bg-white/80 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-slate transition hover:border-accent hover:text-accent"
          >
            {badge.label}
          </a>
        ))}
      </div>
      <h1 class="font-display text-4xl font-semibold text-ink sm:text-5xl">
        Automatización, datos e IA para escalar tu negocio
      </h1>
      <p class="max-w-2xl text-lg text-slate">
        Diseñamos soluciones en marketing digital, machine learning y desarrollo a medida para reducir costos,
        automatizar procesos y mejorar decisiones empresariales.
      </p>
      <div class="flex flex-wrap gap-4">
        <Button variant="primary" href="#contacto">Agenda una llamada</Button>
        <Button variant="ghost" href={whatsappLink} target="_blank" rel="noopener">
          <Icon name="whatsapp" class="h-4 w-4" />
          Escríbenos por WhatsApp
        </Button>
      </div>
    </div>
    <div class="flex-1 reveal" style="--reveal-delay: 120ms;">
      <div class="rounded-3xl border border-white/60 bg-white/80 p-6 shadow-card backdrop-blur">
        <div class="space-y-4">
          <div class="flex items-center gap-3">
            <span class="inline-flex h-12 w-12 items-center justify-center rounded-2xl bg-ink text-white">
              <Icon name="spark" class="h-6 w-6" />
            </span>
            <div>
              <p class="text-sm font-semibold text-ink">Enfoque</p>
              <p class="text-sm text-slate">{site.slogan}</p>
            </div>
          </div>
          <div class="rounded-2xl border border-slate/10 bg-white p-4">
            <p class="text-sm text-slate">Propuesta</p>
            <p class="mt-2 text-lg font-semibold text-ink">Servicios profesionales con impacto claro en negocio.</p>
          </div>
          <div class="rounded-2xl border border-slate/10 bg-white p-4">
            <p class="text-sm text-slate">Resultados</p>
            <p class="mt-2 text-sm text-ink">
              Plan de ejecución con métricas, seguimiento y mejoras continuas.
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

